// This page was removed. Keep a placeholder to avoid import errors if referenced.
// Deleted per request: replaced with placeholder.

import 'package:flutter/material.dart';

class SelectLogPageRemoved extends StatelessWidget {
  const SelectLogPageRemoved({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
